#include<stdio.h>
#include<math.h>

int main(){
    int n, soma = 0;
    scanf("%d", &n);
    int vet[n];
    for(int i = 0; i < n; i++){
        scanf("%d", &vet[i]);
    }
    for(int i = 0; i < n; i++){
        soma = soma + (vet[n -1 - i] * pow(2,i));
    }
    printf("%d", soma);
}
